(()=>{const key="aegeanEssentialsChecklist:v1",boxes=[...document.querySelectorAll("[data-item]")],feedback=document.querySelector("#checklist-feedback");let state={};try{state=JSON.parse(localStorage.getItem(key))||{}}catch{}const save=()=>localStorage.setItem(key,JSON.stringify(Object.fromEntries(boxes.map(box=>[box.dataset.item,box.checked]))));const say=message=>{feedback.textContent=message};boxes.forEach(box=>{box.checked=Boolean(state[box.dataset.item]);box.addEventListener("change",()=>{save();say(`${boxes.filter(item=>item.checked).length} of ${boxes.length} essentials checked.`)})});document.querySelector("#clear-completed").addEventListener("click",()=>{const done=boxes.filter(box=>box.checked);if(!done.length){say("Nothing completed to clear yet.");return}done.forEach(box=>box.checked=false);save();say("Completed items cleared.")});document.querySelector("#reset-checklist").addEventListener("click",()=>{if(!confirm("Reset the entire essentials checklist on this device?"))return;boxes.forEach(box=>box.checked=false);localStorage.removeItem(key);say("Checklist reset on this device.")})})();
