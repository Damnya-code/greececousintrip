# Travel Log photographs

Add optimised WebP or AVIF photographs to the matching `day-01` through `day-08` folder after the trip, then reference them in `data/trip-log.js`.

Images marked `private: true` are not rendered by the website, but anything committed to this public repository is still publicly accessible. Truly private photographs must not be committed here.
